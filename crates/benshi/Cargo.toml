[package]
name = "benshi"
description = "A passive scrobbler: watches the player you already have open."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true

[[bin]]
name = "benshi"
path = "src/main.rs"

[dependencies]
benshi-cli = { path = "../cli" }
benshi-core = { version = "0.1.0", path = "../core" }
benshi-daemon = { path = "../daemon" }
benshi-detect = { version = "0.1.0", path = "../detect" }
benshi-tray = { path = "../tray" }
clap = { version = "4.6.7", features = ["derive"] }
tokio = { version = "1.53.1", features = ["rt-multi-thread"] }

[lints]
workspace = true
